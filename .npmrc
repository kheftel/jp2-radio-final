# react-native-carplay (beta) declares React 17/18 as a peer dependency,
# but Expo SDK 55 uses React 19. The library works fine with React 19 —
# this flag tells npm to allow the mismatch so `npm install` just works.
legacy-peer-deps=true
